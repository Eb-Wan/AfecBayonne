const article = () => {
    return <header><span>Site React</span><button>Se Connecter</button></header>
}

ReactDOM.createRoot(document.getElementById('root')).render(React.createElement(article));

export default article;